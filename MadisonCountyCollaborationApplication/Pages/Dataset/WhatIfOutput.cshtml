@page
@model MadisonCountyCollaborationApplication.Pages.Dataset.WhatIfOutputModel
@{
}
<div class="container mt-4">
    <h2>What-If Analysis Result</h2>

    <div class="mb-4">
        <h4>Expected Outcome:</h4>
        <p>@Model.ExpectedOutcome</p>
    </div>

@*     <div class="mb-4">
        <h4>Variables:</h4>
        <ul>
            @foreach (var variable in Model.IndependentVariables)
            {
                <li>@variable.Replace("_", " ").TrimStart('_'): @Model.WhatIfInputs[variable]</li>
            }
        </ul>
    </div> *@
</div>